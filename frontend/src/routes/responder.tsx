import { createFileRoute } from "@tanstack/react-router"; import { ResponderPage } from "@/components/pages";
export const Route = createFileRoute("/responder")({ head: () => ({ meta: [{ title: "Field Assignment — DisasterShield" }, { name: "description", content: "Mobile field assignment, safe routing, actions and response status." }, { property: "og:title", content: "Field Assignment — DisasterShield" }, { property: "og:description", content: "Mobile field assignment, safe routing, actions and response status." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: ResponderPage });